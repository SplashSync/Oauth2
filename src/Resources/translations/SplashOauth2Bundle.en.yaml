
################################################################
# Connector Configuration Form
form:

    client-id:
        label:      "Client ID"
        help:       "Application Oauth2 Client ID, generated by your application"

    client-secret:
        label:      "Client Secret"
        help:       "Application Oauth2 Client Secret, generated by your application"

################################################################
# Connector Profile Blocks - Connect
connect:
    title:      "Connect to the API"
    intro:      "You are not connected to this API up to now."
    tooltip:    "Create link between your online account and Splash"

################################################################
# Connector Profile Blocks - Refresh
refresh:
    title:      "Refresh API Token"
    tooltip:    "Refresh your connection token"
    expired:    "Current token has expired and will be automatically refreshed"
    expires:    "Current token will expire %TimeToExpire% and will be automatically refreshed"
